import { SlidevMarkdown } from '@slidev/types';
export { detectFeatures, filterDisabled, parse, parseRangeString, prettify, prettifySlide, stringify, stringifySlide } from './core';

declare function load(filepath: string): Promise<SlidevMarkdown>;
declare function save(data: SlidevMarkdown, filepath?: string): Promise<void>;

export { load, save };
